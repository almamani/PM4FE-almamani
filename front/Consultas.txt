Algo similar ocurre tanto en mi formulario de login como en el de register, comencemos por el de login, ya había definido, los estilos en globals.css, sin embargo debería utilizarlo solo en login, y register. Aunque compartirian los mismos estilos me imagino que tengo que hacerlo en módulos css por separado...los estilos globales que ya tengo y debería pasar a los "particulares" son estos: 

form {
  @apply flex flex-col w-1/3 mx-auto;
}

input {
  @apply w-full p-2 mb-2 border border-gray-300 text-xl rounded;
}

y me parece que debería generar uno más para los "p" de los errores.

La aplicación sería similar a lo que hicimos con los links verdad?

Te paso mi componente login por si te sirve de dato adicional:

