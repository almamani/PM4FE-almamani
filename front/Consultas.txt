Tengo un problemilla en esta linea const productsFound: IProduct[] = await getProducts(); // Funciona de 10 pero me marca una advertencia que dice lo siguiente: Type 'IProduct' is missing the following properties from type 'IProduct[]': length, pop, push, concat, and 35 more.ts(2740) Que podrá ser? te pego mi componenente por las dudas:


import { IProduct } from "@/interfaces/IProduct";
import { getProducts } from "@/services/productService";
import Card from "@/components/Card";

const Products = async () => {
  const productsFound: IProduct[] = await getProducts();

  return (
    <main>
      <div>
        <h1>Products</h1>
        <hr />
        <br />
        {productsFound.length > 0 ? (
          <div className="wrapper grid md:grid-cols-4 gap-10">
            {productsFound.map((product: IProduct) => (
              <div key={product.id}>
                <Card product={product} />
              </div>
            ))}
          </div>
        ) : (
          <div className="flex justify-center items-center mt-6">
            <h2 className="text-pink-basic text-center">
              Sorry, we could not load the products. Please try again later.
            </h2>
          </div>
        )}
      </div>
    </main>
  );
};

export default Products;
